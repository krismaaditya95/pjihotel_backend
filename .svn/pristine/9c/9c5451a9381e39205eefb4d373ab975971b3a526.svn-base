package com.mini.project.hotel.services;

import java.util.List;
import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import com.mini.project.hotel.exception.ResourceNotFoundException;
import com.mini.project.hotel.model.Customer;
import com.mini.project.hotel.model.Transaksi;
import com.mini.project.hotel.repository.CustomerRepository;

@Service
public class CustomerService {
	@Autowired
	CustomerRepository custRepo;
	
	
	public List<Customer> getAllCustomers(){
		List<Customer> customers = custRepo.findAll();
		return customers;
	}
	
	public Customer createCustomer(int no_identitas, String nama_customer, String no_telp, String email, String alamat) {
		Customer customer = new Customer(no_identitas, nama_customer, no_telp, email, alamat);
		Customer newCustomer = custRepo.save(customer);
		return newCustomer;
	}
	
//	public Customer getCustomerById(int id_customer){
//		Customer customers = custRepo.getOne(id_customer);
//		return customers;
//	}
	
	public List<Customer> getCustomerById(Integer id_customer){
		List<Customer> customers = custRepo.findByIdCustomer(id_customer);
		return customers;
	}
	
//	public Optional<Customer> getCustomerByIds(int id_customer){
//		Optional<Customer> cust = custRepo.findById(id_customer);
//		return cust;
//	}
}
